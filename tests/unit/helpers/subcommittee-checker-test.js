import { subcommitteeChecker } from 'enlightened-citizenry/helpers/subcommittee-checker';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittee checker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommitteeChecker([42]);
  assert.ok(result);
});
